module.exports.distritosList = function(application, req, res){
	//var dbConfig /*= application.config.dbConnection()*/;
	//var cidadesModel = new application.app.models.distritosDAO(dbConfig);

	/*console.log('ta no controler');

	var oracledb = require('oracledb');
    let dbConfig = {user:"dbo_ccm_pessoas", 
                    password:"dbo_ccm_pessoas", 
					connectString:"172.17.0.113/apolo"};
					

	distritosModel.getDistritos(function(error, result){

		async function run() {
            var connOracleDb = await oracledb.getConnection(dbConfig)

            var query_count = "select count(id) from distritos" 

            connOracleDb.execute(query_count, function(error, result){
				totalPages = result.rows;
				console.log('total pg ' || totalPages);
            });
		}
		//res.render("distritos/distritosList", {distritos : result});
	});	*/
}

